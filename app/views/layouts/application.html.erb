<!DOCTYPE html>
<html lang="vi">
<head>
  <title>NaChiNaCon - Kỷ niệm của bé</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta charset="UTF-8">
  <meta name="description" content="Website lưu giữ kỷ niệm đáng yêu của bé NaChiNaCon">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="min-h-screen font-body text-gray-800 bg-background">
  <!-- Navigation -->
  <nav class="fixed top-0 left-0 right-0 bg-white shadow-md z-50">
    <div class="max-w-6xl mx-auto px-4 md:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <!-- Logo -->
        <%= link_to root_path, class: "flex items-center gap-2 cursor-pointer group" do %>
          <div class="w-10 h-10 rounded-xl flex items-center justify-center shadow group-hover:scale-105 transition-transform duration-300" style="background: linear-gradient(135deg, #C1DDD8 0%, #A8CEC8 100%);">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 text-white">
              <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12z" />
            </svg>
          </div>
          <span class="font-heading font-bold text-xl text-gray-800">NaChiNaCon</span>
        <% end %>

        <!-- Desktop Navigation -->
        <div class="hidden md:flex items-center" style="gap: 2.5rem;">
          <%= link_to 'Trang chủ', root_path, class: "nav-link font-heading font-semibold text-lg #{current_page?(root_path) ? 'text-gray-800 active' : 'text-gray-600 hover:text-gray-800'}" %>
          <%= link_to 'Timeline', timeline_path, class: "nav-link font-heading font-semibold text-lg #{current_page?(timeline_path) ? 'text-gray-800 active' : 'text-gray-600 hover:text-gray-800'}" %>
          <%= link_to 'Milestones', milestones_path, class: "nav-link font-heading font-semibold text-lg #{current_page?(milestones_path) ? 'text-gray-800 active' : 'text-gray-600 hover:text-gray-800'}" %>
          <%= link_to 'Albums', albums_path, class: "nav-link font-heading font-semibold text-lg #{current_page?(albums_path) ? 'text-gray-800 active' : 'text-gray-600 hover:text-gray-800'}" %>
          <% if admin_logged_in? %>
            <%= link_to 'Admin', admin_root_path, class: "nav-link font-heading font-semibold text-lg text-gray-600 hover:text-gray-800" %>
          <% end %>
        </div>

        <!-- Mobile menu button -->
        <button id="mobile-menu-btn" class="md:hidden p-2 rounded-lg hover:bg-gray-100 transition-colors cursor-pointer">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-6 h-6 text-gray-700">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
          </svg>
        </button>
      </div>
    </div>

    <!-- Mobile menu -->
    <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-100">
      <div class="px-4 py-4 space-y-2">
        <%= link_to 'Trang chủ', root_path, class: "block font-heading font-semibold text-lg py-2 #{current_page?(root_path) ? 'text-gray-800' : 'text-gray-600'}" %>
        <%= link_to 'Timeline', timeline_path, class: "block font-heading font-semibold text-lg py-2 #{current_page?(timeline_path) ? 'text-gray-800' : 'text-gray-600'}" %>
        <%= link_to 'Milestones', milestones_path, class: "block font-heading font-semibold text-lg py-2 #{current_page?(milestones_path) ? 'text-gray-800' : 'text-gray-600'}" %>
        <%= link_to 'Albums', albums_path, class: "block font-heading font-semibold text-lg py-2 #{current_page?(albums_path) ? 'text-gray-800' : 'text-gray-600'}" %>
        <% if admin_logged_in? %>
          <%= link_to 'Admin', admin_root_path, class: "block font-heading font-semibold text-lg py-2 text-gray-600" %>
        <% end %>
      </div>
    </div>
  </nav>

  <!-- Flash messages -->
  <% if flash[:notice] %>
    <div class="fixed top-20 right-4 z-50 bg-white px-5 py-3 rounded-xl shadow-lg border border-green-200">
      <div class="flex items-center gap-3">
        <div class="w-7 h-7 rounded-full flex items-center justify-center" style="background: #C0DFD0;">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-4 h-4 text-white">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
        <span class="font-heading font-medium text-gray-700 text-sm"><%= flash[:notice] %></span>
      </div>
    </div>
  <% end %>
  <% if flash[:alert] %>
    <div class="fixed top-20 right-4 z-50 bg-white px-5 py-3 rounded-xl shadow-lg border border-red-200">
      <div class="flex items-center gap-3">
        <div class="w-7 h-7 rounded-full flex items-center justify-center" style="background: #F2C2C2;">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-4 h-4 text-white">
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z" />
          </svg>
        </div>
        <span class="font-heading font-medium text-gray-700 text-sm"><%= flash[:alert] %></span>
      </div>
    </div>
  <% end %>

  <!-- Main content -->
  <main class="pt-24">
    <%= yield %>
  </main>

  <!-- Footer -->
  <footer class="py-12 px-4 md:px-6 lg:px-8 bg-gray-50 border-t border-gray-100">
    <div class="max-w-6xl mx-auto text-center">
      <div class="flex items-center justify-center gap-2 mb-4">
        <div class="w-10 h-10 rounded-xl flex items-center justify-center" style="background: linear-gradient(135deg, #C1DDD8, #A8CEC8);">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5 text-white">
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12z" />
          </svg>
        </div>
        <span class="font-heading font-bold text-lg text-gray-800">NaChiNaCon</span>
      </div>
      <p class="font-accent text-xl text-gray-600 mb-3">"Lớn lên cùng tình yêu, từng kỷ niệm một"</p>
      <p class="font-body text-sm text-gray-400">Made with love for our little prince</p>
    </div>
  </footer>

  <script>
    // Mobile menu toggle
    document.getElementById('mobile-menu-btn')?.addEventListener('click', function() {
      document.getElementById('mobile-menu').classList.toggle('hidden');
    });

    // Auto-hide flash messages
    setTimeout(() => {
      document.querySelectorAll('.fixed.top-24').forEach(el => {
        el.style.opacity = '0';
        el.style.transition = 'opacity 0.5s ease';
        setTimeout(() => el.remove(), 500);
      });
    }, 4000);
  </script>
</body>
</html>
