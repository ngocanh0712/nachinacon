<div class="max-w-2xl mx-auto space-y-6">
  <div>
    <h1 class="font-heading font-bold text-2xl text-gray-800">Chỉnh sửa công thức</h1>
    <p class="font-body text-sm text-gray-500 mt-1 line-clamp-1"><%= @recipe.title %></p>
  </div>

  <div class="bg-white rounded-2xl p-6 shadow-md">
    <%= render 'form', recipe: @recipe %>
  </div>
</div>
