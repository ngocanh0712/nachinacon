<div class="max-w-2xl">
  <div class="mb-6">
    <%= link_to admin_daily_journals_path, class: "inline-flex items-center gap-2 font-heading font-semibold text-sm text-gray-500 hover:text-gray-700 transition-colors" do %>
      <span>←</span>
      <span>Quay lại</span>
    <% end %>
  </div>

  <h1 class="font-heading font-bold text-2xl text-gray-800 mb-6">Sửa nhật ký</h1>

  <%= render 'form', daily_journal: @daily_journal %>
</div>
